#include "main.h"
/**
 * _abs - Computes absolute value of the parameter n
 * @n: value to be computed
 * Return: n
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * (-1);
		return (n);
	}
	else
	{
		return (n)
	}
}
